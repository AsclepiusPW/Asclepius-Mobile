import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  scrollViewContainer: {
    flexGrow: 1,
  },
  container: {
    flex: 1,
    alignItems: "center",
  },
  mapContainer: {
    alignItems: "center",
    width: "100%",
    height: 256,
    marginBottom: 10,
  },
  buttonContainer: {
    marginBottom: 10,
  },
  viewList: {
    width: "100%",
    height: "auto",
    alignItems: "center",
  },
});

export default styles;
