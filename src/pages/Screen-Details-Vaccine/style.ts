import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
  },
  viewList: {
    width: "100%",
    height: "auto",
    alignItems: "center",
  },
});

export default styles;
